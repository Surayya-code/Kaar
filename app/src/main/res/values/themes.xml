<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kaar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/blue</item>
        <item name="android:buttonStyle">@color/blue</item>

      <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>


        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">

    </style>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
    </style>
    <style name="CustomChipBtnStyle" parent="Widget.Material3.Chip.Suggestion">
        <item name="chipBackgroundColor">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="chipStrokeColor">@color/white</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <style name="Theme.Kaar" parent="Base.Theme.Kaar" />
</resources>